# 安全与隐私说明

## API 配置怎样保存

用户输入 API 配置后，需要设置至少 8 位的解锁密码。页面使用 PBKDF2-SHA-256 和随机盐派生 256 位密钥，再使用 AES-GCM 与随机 IV 加密配置。浏览器只保存密文、盐、IV 和版本信息，不保存解锁密码。

加密配置可以导出为 JSON 密文文件，并在另一台设备导入。解锁密码丢失后无法恢复原配置。

## 这套设计能防止什么

- 避免 API Key 以明文写入 localStorage、源码或导出文件；
- 降低其他人直接查看本地存储时获得密钥的风险；
- 页面锁定后清除应用状态中的明文 API Key。

## 这套设计不能防止什么

- 设备已经被恶意软件、浏览器扩展或调试工具控制；
- 用户在钓鱼页面中输入 API Key 或解锁密码；
- 页面解锁并发起请求时，运行环境读取内存或网络请求；
- 第三方 API 服务自身的日志、留存和数据处理风险。

## 报告问题

发现安全问题时，请通过 GitHub 的私密安全报告功能联系仓库所有者。不要在公开 Issue 中粘贴 API Key、解锁密码、完整加密配置或个人学习记录。发现密钥疑似泄露时，应立即在对应服务商后台撤销并重新生成。
